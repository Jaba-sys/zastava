/**
 * MyPeal — Cloud Functions (Фаза 2, НЕ используется сейчас)
 * -----------------------------------------------------------------------
 * Приложение сейчас работает полностью на клиенте + Firestore Security
 * Rules (см. /firestore.rules), чтобы оставаться на бесплатном Firebase
 * Spark-плане — Google требует привязанную карту для ЛЮБЫХ Cloud Functions,
 * даже с нулевым чеком.
 *
 * Если/когда проект перейдёт на план Blaze, сюда стоит вернуть:
 *
 *  1. Автоочистку старой переписки при разрастании базы — периодическая
 *     (scheduled) функция, которая проверяет объём Firestore через
 *     Cloud Monitoring API и удаляет самые старые сообщения пачками.
 *
 *  2. Лимит устройств (макс. 3 одновременных сессии) — при новом входе
 *     проверять users/{uid}/devices, создавать системное сообщение,
 *     уметь отзывать refresh-токены конкретного устройства через
 *     admin.auth().revokeRefreshTokens().
 *
 *  3. Более строгие custom claims для банов (banned → отзыв токена сразу,
 *     а не только на следующий логин).
 *
 * Ранее здесь была реализация отправки 5-значного кода на почту и
 * server-side проверки тегов/заявок в друзья — эта логика перенесена
 * на клиент (Firebase Auth email-link verification + Firestore
 * transactions), см. web/src/pages/*.jsx.
 */
